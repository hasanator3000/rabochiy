# Railway Setup Guide для rabochiy

## ✅ Что уже сделано

1. **Dockerfile создан** - поддерживает SQLite и установлену tsx
2. **БОТ РАБОТАЕТ НА RAILWAY** - статус Online ✨
3. **Database.js обновлён** - использует файловую БД (`./data/telegram.db`)
4. **Переменные окружения установлены** - DB_PATH, TELEGRAM_BOT_TOKEN

## ⚙️ Следующие шаги для сохранения БД между перезагрузками

### Вариант 1: Добавить Volume в Railway UI (РЕКОМЕНДУЕТСЯ)

1. Перейди в Railway → твой проект → rabochiy сервис
2. Нажми на иконку "Volumes" в левой панели (или в Settings найди раздел Volumes)
3. Создай новый Volume с названием `data`
4. Установи точку монтирования `/data`
5. Сохрани и пересоберись

### Вариант 2: Использовать Config File

Добавь в `railway.json` раздел volumes:

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "context": "."
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyMaxRetries": 10,
    "volumes": [
      {
        "name": "data",
        "mountPath": "/data"
      }
    ]
  }
}
```

## 📋 Проверка работы

После настройки Volume:

1. Отправь боту команду `/start`
2. Проверь в Railway логи - должно быть:
   - `✅ База данных инициализирована`
   - `✅ Бот успешно инициализирован`
3. Сыграй в игру и выиграй
4. Проверь, что промокод отправлен
5. Перезагрузи сервис на Railway
6. Убедись, что БД не потеряла данные (сыграй ещё раз и проверь историю)

## 🔧 Если что-то не работает

- Посмотри логи в Railway → rabochiy → Deployment Logs
- Убедись, что TELEGRAM_BOT_TOKEN установлен в Variables
- Проверь, что Volume монтируется в `/data` (а не `/app/data`)
- Убедись, что `/data` директория создана в Dockerfile (есть команда `RUN mkdir -p /app/data`)

## 📝 Переменные окружения

Обязательные:
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `TELEGRAM_WEB_APP_URL` - URL игры (например, https://rabochiy-five.vercel.app)

Опциональные:
- `DB_PATH` - путь к БД (по умолчанию `/data/telegram.db`)
- `PORT` - порт сервера (по умолчанию 3000)

## 🎯 Результат

После всех этих шагов:
- ✅ БД сохраняется между перезагрузками
- ✅ Все пользователи получают промокоды
- ✅ История игр сохраняется
- ✅ Бот работает 24/7 на Railway
