# 📄 **PRD — Проект «Игра Крестики-Нолики с Телеграм-уведомлениями»**

## 1. **Executive Summary**

Проект представляет собой веб-игру «Крестики-нолики», в которой игрок (женщина 25–40 лет) играет против компьютера.
При победе игрока генерируется случайный 5-значный промокод, который отображается на экране и отправляется в Telegram-бот с сообщением «Победа! Промокод выдан: [код]».
При проигрыше отправляется сообщение «Проигрыш» и предлагается сыграть снова.
Цель — продемонстрировать навыки разработки для прохождения тестового задания.

---

## 2. **Problem Statement**

Кандидату необходимо продемонстрировать умение:

* создавать UI в соответствии с заданной аудиторией;
* реализовывать игровую логику;
* интегрировать React-приложение с Telegram-ботом;
* генерировать данные (промокоды);
* построить полноценный рабочий прототип.

Проект выполняется исключительно для прохождения собеседования.

---

## 3. **Goals & Success Metrics**

### 🎯 Цели:

* Создать полностью рабочую игру.
* Выполнить все условия ТЗ.
* Подготовить развёрнутую ссылку для проверки.

### 📊 Метрика успеха:

* **Успешное прохождение собеседования.**

---

## 4. **Target Users**

Основная аудитория игрока: женщины 25–40 лет.

### Ключевые предпочтения:

* мягкие тона (пастельные, нежные оттенки);
* минимализм и приятная анимация;
* аккуратные, закруглённые элементы;
* визуальный комфорт, интуитивность интерфейса.

---

## 5. **Product Overview**

Веб-страница содержит:

1. игровое поле 3×3;
2. элементы UI, адаптированные под женскую аудиторию (цветовая палитра, шрифты, плавные анимации);
3. игровую логику:

   * пользователь делает ход «X»;
   * компьютер автоматически делает ход «O» (оптимальная или простая логика);
4. обработку исхода партии:

   * при победе → промокод + уведомление в Telegram;
   * при поражении → уведомление + предложение сыграть ещё.

Telegram-бот создаётся разработчиком самостоятельно.

---

## 6. **Functional Requirements**

### 6.1 Основные функции

| ID   | Требование                                | Критерий приёмки                                                                 |
| ---- | ----------------------------------------- | -------------------------------------------------------------------------------- |
| FR-1 | Поле 3×3, реагирующее на клики            | Пользователь может поставить X в пустую клетку                                   |
| FR-2 | Компьютер делает ход автоматически        | Ход совершается после хода пользователя                                          |
| FR-3 | Обнаружение победы/поражения/ничьей       | Игра корректно определяет состояние                                              |
| FR-4 | Генерация случайного 5-значного промокода | Код состоит из цифр и букв A–Z                                                   |
| FR-5 | Показ промокода на экране                 | Код отображается красиво и заметно                                               |
| FR-6 | Отправка данных в Telegram-бот            | В случае победы: «Победа! Промокод выдан: X1234», в случае поражения: «Проигрыш» |
| FR-7 | Кнопка «Сыграть ещё» при проигрыше        | При нажатии игра перезапускается                                                 |
| FR-8 | Адаптация UI под женскую аудиторию        | Дизайн соответствует описанным визуальным критериям                              |

---

## 7. **Non-Functional Requirements**

### 7.1 Performance

* Загрузка страницы ≤ 1 сек.
* Отклик на ход игрока ≤ 150 мс.

### 7.2 Security

* Telegram-API токен не хранится на клиенте — отправка идёт через прокси-сервер (Node.js) или облачный function endpoint.
* Промокоды генерируются на клиенте, но отправка в Telegram — только через backend-слой.

### 7.3 Scalability

* Приложение должно работать без деградации при большом числе игроков (хоть это и не требуется для ТЗ, но важно для качества).

### 7.4 Reliability

* Если Telegram недоступен → игра должна показывать сообщение: «Ошибка отправки уведомления».

### 7.5 UX/UI

* Плавные анимации.
* Приятная женская цветовая палитра (пастельные оттенки розового, бежевого, пудрового).
* Шрифт: Inter / Rubik / Nunito.

---

## 8. **User Experience**

### 8.1 Основной игровой поток

1. Пользователь открывает страницу.
2. Видит приветственный экран с кнопкой «Начать игру».
3. На экране появляется поле 3×3.
4. Пользователь нажимает на свободную клетку → ставится X.
5. ИИ делает ответный ход.
6. Алгоритм проверяет состояние игры:

   * Win → промокод → отправка в Telegram → экран победы.
   * Lose → отправка в Telegram → экран «Попробуйте снова» → кнопка.
   * Draw → сообщение «Ничья» → кнопка «Играть снова».

---

## 9. **Technical Considerations**

### 9.1 Стек разработки

* **React (CRA или Vite)**
* **JavaScript**
* **Node.js (мини-backend для Telegram API)**
* Хостинг: любой быстрый (Vercel / Netlify / Replit / GitHub Pages + backend proxy на Vercel Functions)

### 9.2 Интеграция Telegram

* Используется Telegram Bot API (sendMessage).
* Backend принимает POST запрос:

  ```
  POST /send
  {
    "status": "win" | "lose",
    "code": "ABCDE"
  }
  ```
* На backend отправляется сообщение в Telegram.

### 9.3 Архитектура

```
Frontend (React)
   |
   | fetch() → POST /send
   |
Backend (Node.js / serverless)
   |
Telegram Bot API
```

---

## 10. **Timeline & Milestones (2 дня)**

| День       | Задачи                                                                      |
| ---------- | --------------------------------------------------------------------------- |
| **День 1** | Разработка UI, логика игры, генерация промокодов, базовое состояние игры    |
| **День 2** | Создание Telegram-бота, backend для отправки сообщений, интеграция + деплой |

---

## 11. **Risks & Assumptions**

### Риски:

* Telegram API может блокировать запросы при неверной конфигурации.
* При размещении только статического HTML невозможно безопасно использовать Telegram токен → нужен backend.
* AI логика может быть слишком сложной → для ТЗ достаточно простого «рандомного хода» или упрощённой стратегии.

### Предположения:

* Дизайн остаётся на усмотрение разработчика, главное — соответствовать женской аудитории 25–40.
* Пользователь не требует продвинутой AI-логики.
* Telegram-бот создаётся самостоятельно и доступен для тестирования через токен.

---

# 📌 Готово!

Если хотите, могу:

✅ создать **полный дизайн-макет** (Figma)
✅ написать **полный код React + backend**
✅ собрать рабочий деплой на Vercel / Netlify
✅ подготовить ссылку для тестового задания

Какой следующий шаг?

